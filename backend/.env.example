# Server
PORT=8080

# Database (Prisma 7 with PostgreSQL adapter)
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# Redis (BullMQ: email queue, DLQ, notifications; local or AWS ElastiCache)
REDIS_URL=redis://127.0.0.1:6379

# JWT
JWT_SECRET=your-secret-key-change-in-production

# SMTP (Gmail or other)
SMTP_USER=mavinash422@gmail.com
SMTP_PASS=yjrw ofgv zlfm qkau

# Admin email for dead-letter alerts (defaults to SMTP_USER if unset)
ADMIN_EMAIL=mavinash422@gmail.com

# Frontend URL (for reset link)
FRONTEND_URL=http://localhost:5173

# CORS
CORS_ORIGIN=http://localhost:5173