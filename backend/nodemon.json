{
  "watch": ["src", "index.ts", "config", "prisma/schema.prisma"],
  "ext": "ts,js,json",
  "ignore": ["src/**/*.test.ts", "node_modules", "dist"],
  "exec": "ts-node --transpile-only index.ts",
  "env": {
    "NODE_ENV": "development"
  },
  "delay": "1000"
}
